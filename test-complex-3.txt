Complex test with JavaScript syntax:
function test() {
  const obj = {
    "key": "value with 'quotes'",
    "nested": {
      "array": [1, 2, 3],
      "string": "test \n with \t escapes"
    }
  };
  return obj;
}
