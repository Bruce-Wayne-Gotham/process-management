services:
  web:
    build:
      context: ./nextjs
      args: {}
    ports:
      - "3000:3000"
    env_file:
      - ./nextjs/.env.local
    environment:
      NODE_ENV: production
      # DATABASE_URL is provided by Render in production
    restart: unless-stopped
